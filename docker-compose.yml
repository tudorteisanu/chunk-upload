services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    command: pm2-runtime start dist/index.js --name chunk-upload -i max --max-memory-restart 500M
